# 内部文档请求与证据收集系统

> **Petición de Documentación y Recopilación de Evidencias**

## 目录

- [一、项目简介](#一项目简介)
- [二、系统架构](#二系统架构)
- [三、技术栈](#三技术栈)
- [四、项目结构](#四项目结构)
- [五、数据模型](#五数据模型)
- [六、核心功能](#六核心功能)
- [七、环境配置](#七环境配置)
- [八、本地开发部署](#八本地开发部署)
- [九、Docker 生产部署](#九docker-生产部署)
- [十、使用指南](#十使用指南)
- [十一、API 接口文档](#十一api-接口文档)
- [十二、Power Automate 集成](#十二power-automate-集成)
- [十三、安全机制](#十三安全机制)
- [十四、状态机设计](#十四状态机设计)
- [十五、常见问题](#十五常见问题)

---

## 一、项目简介

### 1.1 背景

本系统专为专业服务公司（四大会计师事务所 / Forvis Mazars 等）设计，用于管理内部控制审计过程中向经理或合伙人发起的结构化文档请求。系统的设计初衷是：**在没有 IT 部门支持、没有 Azure AD / Graph API 权限的环境下**，仍能完成从请求发送到证据归档的全流程管理。

### 1.2 解决的问题

| 痛点 | 解决方案 |
|------|---------|
| 手动通过邮件追踪文档请求 | 结构化的活动（Campaña）管理系统 |
| 无法确认对方是否收到 / 查看请求 | 令牌化访问链接 + 状态追踪 |
| 文件散落在各处，缺乏统一归档 | SharePoint 自动归档（通过 Power Automate） |
| 催促回复费时费力 | 3 级自动提醒升级机制 |
| 无审计痕迹 | 不可变审计日志记录所有操作 |
| 没有 IT 授权就无法使用企业 API | Power Automate 作为授权代理中间件 |

### 1.3 核心流程概览

```
创建活动 → 添加请求 → 发送链接 → 接收方上传证据 → 审核验证 → 关闭归档
   │                      │              │              │
   └── 草稿阶段 ──────────┘              │              │
                          自动提醒 ←──────┘              │
                          (3 级升级)        驳回 → 补正 ──┘
```

---

## 二、系统架构

### 2.1 三层架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Next.js 14)                      │
│              http://localhost:3001                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐  │
│  │ 活动管理  │ │ 请求详情  │ │ 审计日志  │ │ 接收方门户  │  │
│  └──────────┘ └──────────┘ └──────────┘ └────────────┘  │
└─────────────────────┬───────────────────────────────────┘
                      │ API 代理 (/api/* → :3002)
┌─────────────────────▼───────────────────────────────────┐
│                   后端 (Express + TypeScript)              │
│              http://localhost:3002                        │
│  ┌──────┐ ┌──────────┐ ┌────────┐ ┌────────────────┐    │
│  │ 认证  │ │ 活动服务   │ │ 令牌   │ │ 通知 (Mock/PA) │    │
│  │ JWT  │ │ 证据服务   │ │ OTP   │ │ 报告 (PDF)     │    │
│  └──────┘ └──────────┘ └────────┘ └────────────────┘    │
└─────────────────────┬───────────────────────────────────┘
                      │ Prisma ORM
┌─────────────────────▼───────────────────────────────────┐
│              PostgreSQL 16 (Docker)                       │
│              localhost:5433                               │
│  8 张数据表 · 11 个索引 · JSONB 策略字段                      │
└─────────────────────────────────────────────────────────┘
         │
         │ Power Automate Webhooks (可选)
         ▼
┌─────────────────────┐
│ SharePoint / Outlook │
│   邮件发送 · 文件归档    │
└─────────────────────┘
```

### 2.2 架构决策要点

| 决策 | 原因 |
|------|------|
| Power Automate 作为中间件 | 无需 IT 授权即可访问 Microsoft 365 |
| JWT httpOnly Cookie | 安全的无状态认证，防 XSS |
| 令牌化 URL 访问 | 接收方无需注册账号 |
| OTP 二次验证 | 确保只有邮件所有者可访问 |
| Prisma + PostgreSQL | 类型安全 ORM + 强事务支持 |
| Next.js App Router | 现代化文件路由 + 服务端渲染 |

---

## 三、技术栈

### 3.1 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 20+ | 运行时 |
| Express | 4.21 | Web 框架 |
| TypeScript | 5.7 | 类型安全 |
| Prisma | 6.4 | ORM 与数据库迁移 |
| PostgreSQL | 16 | 关系型数据库 |
| JWT (jsonwebtoken) | 9.0 | 身份认证 |
| bcryptjs | 2.4 | 密码加密 |
| Winston | 3.17 | 日志系统 |
| Zod | 3.24 | 请求校验 |
| Multer | 1.4 | 文件上传处理 |
| PDFKit | 0.15 | PDF 报告生成 |
| Helmet | 8.0 | HTTP 安全头 |
| express-rate-limit | 7.5 | 速率限制 |

### 3.2 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 14.2 | React 框架 (App Router) |
| React | 18.3 | UI 库 |
| Tailwind CSS | 3.4 | 样式系统 |
| Zustand | 5.0 | 状态管理 |
| React Hook Form | 7.54 | 表单处理 |
| Zod | 3.24 | 表单校验 |
| Recharts | 2.15 | 数据图表 |
| React Dropzone | 14.3 | 拖拽上传 |
| Lucide React | 0.468 | 图标库 |
| @tanstack/react-table | 8.21 | 数据表格 |
| date-fns | 4.1 | 日期处理 |
| Axios | 1.7 | HTTP 请求 |

### 3.3 基础设施

| 技术 | 用途 |
|------|------|
| Docker + Docker Compose | 容器化部署 |
| Power Automate | Microsoft 365 集成中间件 |
| SharePoint | 文件永久归档 |

---

## 四、项目结构

```
peticion-documentacion/
├── 📄 package.json              # 后端依赖与脚本
├── 📄 tsconfig.json             # TypeScript 配置
├── 📄 docker-compose.yml        # Docker 编排（数据库 + 应用）
├── 📄 Dockerfile                # 多阶段构建（生产部署）
├── 📄 .env                      # 环境变量（不提交到 Git）
├── 📄 .env.example              # 环境变量模板
├── 📄 .gitignore                # Git 忽略规则
├── 📄 项目说明.md                # ← 本文件
│
├── 📂 prisma/
│   ├── 📄 schema.prisma         # 数据模型定义（8 张表）
│   ├── 📄 seed.ts               # 数据库初始化种子数据
│   └── 📂 migrations/           # 自动生成的迁移文件
│
├── 📂 src/                      # 后端源码
│   ├── 📄 server.ts             # Express 入口文件
│   ├── 📂 config/               # 配置管理
│   │   ├── 📄 index.ts          # 环境变量校验与导出
│   │   └── 📄 database.ts       # Prisma 客户端单例
│   ├── 📂 middleware/            # Express 中间件
│   │   ├── 📄 auth.ts           # JWT 认证 + 门户令牌认证
│   │   ├── 📄 errorHandler.ts   # 全局错误处理
│   │   ├── 📄 rateLimiter.ts    # 速率限制（3 个级别）
│   │   └── 📄 validation.ts     # Zod 校验中间件工厂
│   ├── 📂 routes/               # 路由层（6 组路由）
│   │   ├── 📄 auth.routes.ts    # 登录 / 登出 / 用户
│   │   ├── 📄 campaign.routes.ts # 活动 CRUD + 激活
│   │   ├── 📄 request.routes.ts  # 请求管理 + 发送 + 关闭
│   │   ├── 📄 evidence.routes.ts # 证据验证 / 驳回 / 补正
│   │   ├── 📄 portal.routes.ts   # 接收方门户（令牌 + OTP）
│   │   └── 📄 audit.routes.ts    # 审计日志查询
│   ├── 📂 services/             # 业务逻辑层（10 个服务）
│   │   ├── 📄 audit.service.ts        # 审计日志
│   │   ├── 📄 campaign.service.ts     # 活动管理
│   │   ├── 📄 evidence.service.ts     # 证据审核
│   │   ├── 📄 notification.service.ts # 通知（Mock / PA）
│   │   ├── 📄 otp.service.ts          # OTP 一次性密码
│   │   ├── 📄 reminder.service.ts     # 提醒调度
│   │   ├── 📄 report.service.ts       # PDF 报告
│   │   ├── 📄 request.service.ts      # 请求流程
│   │   ├── 📄 submission.service.ts   # 提交处理
│   │   └── 📄 token.service.ts        # 令牌生成与校验
│   ├── 📂 types/
│   │   └── 📄 index.ts          # TypeScript 类型定义
│   └── 📂 utils/
│       ├── 📄 errors.ts         # 自定义错误类
│       └── 📄 logger.ts         # Winston 日志配置
│
├── 📂 client/                   # 前端源码（Next.js 14）
│   ├── 📄 package.json          # 前端依赖
│   ├── 📄 next.config.js        # Next.js 配置（API 代理）
│   ├── 📄 tsconfig.json         # TypeScript 配置
│   ├── 📄 tailwind.config.ts    # Tailwind 主题配置
│   ├── 📄 postcss.config.js     # PostCSS 配置
│   ├── 📂 app/                  # App Router 页面
│   │   ├── 📄 layout.tsx        # 根布局（HTML、元数据）
│   │   ├── 📄 page.tsx          # 首页（重定向到 /campaigns）
│   │   ├── 📄 globals.css       # 全局样式 + 自定义组件类
│   │   ├── 📂 login/            # 登录页
│   │   │   └── 📄 page.tsx
│   │   ├── 📂 submit/[token]/   # 接收方门户（独立布局）
│   │   │   └── 📄 page.tsx
│   │   └── 📂 (dashboard)/      # 认证后路由组
│   │       ├── 📄 layout.tsx    # 仪表板布局（侧边栏 + 头部）
│   │       ├── 📂 campaigns/    # 活动管理
│   │       │   ├── 📄 page.tsx       # 活动列表（筛选、搜索、分页）
│   │       │   ├── 📂 new/           # 新建活动表单
│   │       │   └── 📂 [id]/          # 活动详情
│   │       │       ├── 📄 page.tsx        # 活动概览 + KPI
│   │       │       ├── 📂 requests/       # 请求管理
│   │       │       └── 📂 dashboard/      # 数据看板
│   │       ├── 📂 requests/[id]/ # 请求详情 + 证据审核
│   │       ├── 📂 audit/        # 审计日志查看器
│   │       └── 📂 admin/users/  # 用户管理
│   ├── 📂 components/layout/    # 布局组件
│   │   ├── 📄 Sidebar.tsx       # 可折叠侧边栏导航
│   │   ├── 📄 Header.tsx        # 顶部栏（用户信息、登出）
│   │   └── 📄 Toast.tsx         # 消息提示组件
│   ├── 📂 lib/                  # 工具库
│   │   ├── 📄 api.ts            # Axios 实例（withCredentials）
│   │   └── 📄 utils.ts          # 通用工具函数
│   └── 📂 store/
│       └── 📄 index.ts          # Zustand 状态管理
│
├── 📂 docs/                     # 原始设计文档
│   ├── 📄 README.md             # 项目规格说明（英文）
│   ├── 📄 README_CN.md          # 项目规格说明（中文）
│   ├── 📄 ARCHITECTURE.md       # 技术架构文档（英文）
│   └── 📄 ARCHITECTURE_CN.md    # 技术架构文档（中文）
│
├── 📂 Diagrama de uso/          # 用例图
└── 📂 Diagrama tecnico/         # 技术架构图
```

---

## 五、数据模型

### 5.1 ER 关系图

```
User (1) ──── (N) Campaign ──── (N) Request ──── (N) EvidenceItem
  │                                    │                 │
  │                                    │                 │
  └── OWNER / ADMIN / VIEWER      (N) Submission    (N) SubmissionFile
                                       │                 │
                                       └────── (N) ──────┘

EvidenceTemplate (1) ──── (N) EvidenceItem

AuditLog（独立表，记录所有操作）
```

### 5.2 数据表说明

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `users` | 系统用户 | email, role (OWNER/ADMIN/VIEWER), password_hash |
| `campaigns` | 文档请求活动 | control_code, status, reminder_policy (JSONB), escalation_policy (JSONB) |
| `requests` | 单个请求 | recipient_email, deadline, token_hash, escalation_level |
| `evidence_templates` | 证据模板库 | name, type (PDF/EXCEL/WORD/IMAGE...), default_instructions |
| `evidence_items` | 请求中的具体证据项 | is_mandatory, status, rejection_reason, file_sp_path |
| `submissions` | 接收方的提交记录 | submitted_by_email, ip_address, notes |
| `submission_files` | 上传的文件记录 | original_filename, mime_type, size_bytes, sp_path |
| `audit_logs` | 审计日志（不可变） | entity_type, action (24种), actor_email, details (JSONB) |

### 5.3 枚举类型

**活动状态** (CampaignStatus):
```
DRAFT → ACTIVE → COMPLETED → ARCHIVED
```

**请求状态** (RequestStatus):
```
DRAFT → SENT → IN_PROGRESS → PARTIAL → SUBMITTED → REVIEW → READY_TO_CLOSE → CLOSED
                                                                              ↗
                                                              OVERDUE ────────
```

**证据状态** (EvidenceStatus):
```
PENDING → SUBMITTED → VALIDATED
                    → REJECTED → (补正) → PENDING
```

**审计动作** (AuditAction): 共 24 种，覆盖活动、请求、证据、提交、令牌、OTP、文件、用户等所有操作。

---

## 六、核心功能

### 6.1 活动管理（Campañas）

- 创建活动：设置名称、控制代码、起止日期、提醒策略、升级策略
- 活动状态流转：草稿 → 激活 → 完成 → 归档
- 自动完成检测：当所有请求关闭时，活动自动转为已完成
- KPI 仪表板：总请求数、各状态统计、完成率、逾期率、平均响应天数
- PDF 报告：一键导出活动完整报告

### 6.2 请求管理（Solicitudes）

- 批量创建请求：指定接收人、委托人、截止日期、证据清单
- 发送请求：生成唯一令牌 URL，通过邮件（或 Power Automate）发送
- 3 级提醒升级：
  - **1 级**：仅通知接收人
  - **2 级**：抄送委托人
  - **3 级**：抄送上级（如合伙人）
- 请求关闭：验证所有必填证据均已通过审核后关闭

### 6.3 证据审核（Evidencias）

- 查看每个请求下的证据项及其状态
- 验证通过：标记为已验证，检查是否全部通过 → 请求就绪
- 驳回：填写驳回原因（≥5 字符），请求回退到处理中
- 补正：将驳回的证据重置为待提交状态

### 6.4 接收方门户（Portal del Destinatario）

- 令牌化访问：无需注册账号，通过唯一链接访问
- OTP 验证：可选的 6 位一次性密码，10 分钟有效，最多 3 次尝试
- 文件上传：按证据项逐个拖拽上传，支持备注
- 状态查看：实时查看每个证据项的当前状态

### 6.5 审计日志（Registro de Auditoría）

- 不可变记录：所有系统操作自动记录
- 高级筛选：按实体类型、操作类型、活动、日期范围过滤
- JSON 详情：展开查看操作的完整参数

### 6.6 用户管理

- 三种角色：管理员 (ADMIN)、负责人 (OWNER)、观察者 (VIEWER)
- 权限控制：OWNER 只看自己的活动，ADMIN 看所有活动

---

## 七、环境配置

### 7.1 环境变量说明

复制 `.env.example` 为 `.env`，然后根据实际情况修改：

```bash
cp .env.example .env
```

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| **应用配置** | | |
| `APP_PORT` | `3002` | 后端服务端口 |
| `APP_BASE_URL` | `http://localhost:3002` | 后端基础 URL |
| `NODE_ENV` | `development` | 运行环境 (development / production) |
| **安全配置** | | |
| `JWT_SECRET` | (必须修改) | JWT 签名密钥，生产环境请使用强随机字符串 |
| `JWT_EXPIRES_IN` | `8h` | JWT 过期时间 |
| `TOKEN_SECRET` | (必须修改) | 接收方令牌签名密钥 |
| `TOKEN_EXPIRY_DAYS` | `7` | 接收方令牌有效天数 |
| **数据库配置** | | |
| `DB_HOST` | `localhost` | PostgreSQL 主机 |
| `DB_PORT` | `5433` | PostgreSQL 端口 |
| `DB_NAME` | `doc_requests` | 数据库名 |
| `DB_USER` | `app_user` | 数据库用户 |
| `DB_PASSWORD` | `changeme` | 数据库密码（生产环境必须修改） |
| `DATABASE_URL` | (自动拼接) | Prisma 连接字符串 |
| **Power Automate** | | |
| `PA_FLOW_SEND_REQUESTS` | (Webhook URL) | Flow 1: 发送请求邮件 |
| `PA_FLOW_SEND_OTP` | (Webhook URL) | Flow 2: 发送 OTP 验证码 |
| `PA_FLOW_ARCHIVE_FILES` | (Webhook URL) | Flow 4: 归档文件到 SharePoint |
| `PA_FLOW_REMINDERS` | (Webhook URL) | Flow 3: 发送提醒 |
| **SharePoint** | | |
| `SP_SITE_URL` | (SharePoint 站点 URL) | SharePoint 站点地址 |
| `SP_LIBRARY_NAME` | `Evidencias` | SharePoint 文档库名称 |
| **OTP 配置** | | |
| `OTP_ENABLED` | `true` | 是否启用 OTP 二次验证 |
| `OTP_EXPIRY_MINUTES` | `10` | OTP 有效期（分钟） |
| `OTP_MAX_ATTEMPTS` | `3` | OTP 最大尝试次数 |
| **文件配置** | | |
| `UPLOAD_DIR` | `uploads` | 上传文件临时目录 |
| `MAX_FILE_SIZE_MB` | `100` | 单文件最大大小 (MB) |

> ⚠️ **安全提醒**：生产环境务必修改 `JWT_SECRET`、`TOKEN_SECRET`、`DB_PASSWORD`，使用至少 32 位的随机字符串。

---

## 八、本地开发部署

### 8.1 前置条件

- **Node.js** 20+ ([下载](https://nodejs.org/))
- **Docker Desktop** ([下载](https://www.docker.com/products/docker-desktop))
- **Git** (可选，用于版本控制)

### 8.2 步骤一：启动数据库

```bash
# 确保 Docker Desktop 已启动
docker-compose up -d db
```

等待数据库就绪（约 5-10 秒）：

```bash
docker ps --filter "name=peticiondedocumentacion-db" --format "{{.Status}}"
# 输出应包含 (healthy)
```

### 8.3 步骤二：安装后端依赖

```bash
npm install
```

### 8.4 步骤三：初始化数据库

```bash
# 生成 Prisma 客户端
npx prisma generate

# 执行数据库迁移（创建所有表）
npx prisma migrate dev --name init

# 填充初始数据（2 个用户 + 3 个模板 + 1 个示例活动）
npx prisma db seed
```

### 8.5 步骤四：启动后端

```bash
npm run dev
```

输出：`Servidor iniciado en puerto 3002`

### 8.6 步骤五：安装前端依赖并启动

```bash
cd client
npm install
npm run dev
```

输出：`Ready in X.Xs` → 可访问 http://localhost:3001

### 8.7 步骤六：验证

打开浏览器访问 **http://localhost:3001/login**，使用以下账号登录：

| 邮箱 | 密码 | 角色 |
|------|------|------|
| `admin@firma.com` | `admin123` | 管理员 |
| `responsable@firma.com` | `admin123` | 负责人 |

### 8.8 其他开发工具

```bash
# 打开 Prisma Studio（数据库可视化管理）
npx prisma studio
# → 浏览器打开 http://localhost:5555

# 查看后端日志
# 开发环境自动输出彩色日志到控制台

# 检查 API 健康状态
curl http://localhost:3002/health
# → {"status":"ok","timestamp":"..."}
```

---

## 九、Docker 生产部署

### 9.1 完整 Docker 部署

```bash
# 1. 修改 .env 中的生产配置
#    - NODE_ENV=production
#    - 修改所有密钥为强随机字符串
#    - 配置 Power Automate Webhook URL

# 2. 一键启动（数据库 + 应用）
docker-compose up -d

# 3. 查看运行状态
docker-compose ps

# 4. 查看日志
docker-compose logs -f app
```

### 9.2 Docker Compose 服务

| 服务 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| `db` | postgres:16-alpine | 5433:5432 | PostgreSQL 数据库 |
| `app` | 自构建 (Dockerfile) | 3002:3000 | 后端应用 |

### 9.3 Dockerfile 说明

采用**多阶段构建**优化镜像体积：

```
阶段 1 (builder)：
  - 安装依赖 → 生成 Prisma 客户端 → 编译 TypeScript

阶段 2 (runner)：
  - 复制编译产物 → 创建非 root 用户 → 启动时自动迁移数据库
```

### 9.4 数据持久化

| Docker Volume | 用途 |
|---------------|------|
| `pgdata` | PostgreSQL 数据文件 |
| `uploads` | 用户上传的文件（临时存储） |

### 9.5 生产环境安全清单

- [ ] 修改 `JWT_SECRET` 为 32+ 位随机字符串
- [ ] 修改 `TOKEN_SECRET` 为不同的 32+ 位随机字符串
- [ ] 修改 `DB_PASSWORD` 为强密码
- [ ] 设置 `NODE_ENV=production`
- [ ] 配置 HTTPS（反向代理如 Nginx/Caddy）
- [ ] 配置 Power Automate Webhook 真实 URL
- [ ] 修改 CORS 允许的域名
- [ ] 备份策略：定期备份 PostgreSQL 数据

---

## 十、使用指南

### 10.1 内部用户操作流程

#### 第一步：创建活动

1. 登录系统，进入「活动列表」页面
2. 点击「+ 新建活动」
3. 填写基本信息：
   - **名称**：如 "2026年度人力资源控制审计"
   - **控制代码**：如 "HR-FCTRL-1"
   - **描述**：活动说明
   - **起止日期**
4. 配置提醒策略：
   - 提醒频率（天）
   - 最大提醒次数
   - 发送提醒前等待天数
5. 配置升级策略：
   - 2 级升级阈值（几次提醒后抄送委托人）
   - 3 级升级阈值（几次提醒后抄送上级）
   - 上级邮箱
6. 保存（活动处于草稿状态）

#### 第二步：添加请求

1. 进入活动详情 → 「请求管理」
2. 填写每个请求：
   - 接收人姓名和邮箱
   - 委托人邮箱（可选）
   - 截止日期
   - 需要的证据清单（名称、类型、是否必填、说明）
3. 可添加多个请求

#### 第三步：激活并发送

1. 返回活动详情，点击「激活活动」
2. 在请求列表中，点击每个请求的「发送」按钮
3. 系统生成唯一令牌 URL，发送邮件通知接收人
4. 请求状态变为「已发送」

#### 第四步：审核证据

1. 接收方通过门户上传证据后，请求状态变为「已提交」或「审核中」
2. 进入请求详情，逐项审核证据：
   - ✅ **验证通过**：标记为已验证
   - ❌ **驳回**：填写驳回原因，证据退回
   - 🔄 **补正**：将驳回的证据重置为待提交

#### 第五步：关闭请求

- 当所有必填证据通过验证，请求变为「就绪」状态
- 点击「关闭」，系统发送关闭通知
- 当活动内所有请求关闭，活动自动变为「已完成」

### 10.2 接收方操作流程

1. 收到邮件，点击唯一链接
2. 如启用 OTP，输入邮件中收到的 6 位验证码
3. 查看需要提交的证据清单
4. 为每个证据项上传文件（拖拽或点击选择）
5. 填写备注（可选）
6. 点击「提交」
7. 如证据被驳回，重新进入门户上传更正后的文件

---

## 十一、API 接口文档

### 11.1 认证接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/auth/login` | 登录 | 公开 |
| POST | `/api/auth/refresh` | 刷新令牌 | 已认证 |
| POST | `/api/auth/logout` | 登出 | 已认证 |
| GET | `/api/auth/me` | 获取当前用户 | 已认证 |
| GET | `/api/auth/users` | 获取用户列表 | 仅 ADMIN |

### 11.2 活动接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/campaigns` | 创建活动 | OWNER/ADMIN |
| GET | `/api/campaigns` | 获取活动列表 | 已认证 |
| GET | `/api/campaigns/:id` | 获取活动详情 | 已认证 |
| PUT | `/api/campaigns/:id` | 更新活动（仅草稿） | OWNER/ADMIN |
| POST | `/api/campaigns/:id/activate` | 激活活动 | OWNER/ADMIN |
| POST | `/api/campaigns/:id/requests` | 批量创建请求 | OWNER/ADMIN |
| GET | `/api/campaigns/:id/requests` | 获取活动下的请求 | 已认证 |
| GET | `/api/campaigns/:id/dashboard` | 获取活动 KPI | 已认证 |
| GET | `/api/campaigns/:id/report` | 下载 PDF 报告 | 已认证 |

### 11.3 请求接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/requests/pending-reminders` | 获取待提醒请求 | OWNER/ADMIN |
| GET | `/api/requests/:id` | 获取请求详情 | 已认证 |
| PUT | `/api/requests/:id` | 更新请求（仅草稿） | OWNER/ADMIN |
| POST | `/api/requests/:id/send` | 发送请求 | OWNER/ADMIN |
| POST | `/api/requests/:id/close` | 关闭请求 | OWNER/ADMIN |
| POST | `/api/requests/:id/reminder-sent` | PA 回调 | OWNER/ADMIN |

### 11.4 证据接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/evidences/request/:requestId` | 获取请求的证据列表 | 已认证 |
| POST | `/api/evidences/:id/validate` | 验证证据 | OWNER/ADMIN |
| POST | `/api/evidences/:id/reject` | 驳回证据 | OWNER/ADMIN |
| POST | `/api/evidences/:id/subsanation` | 证据补正 | OWNER/ADMIN |

### 11.5 门户接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/submit/:token` | 获取请求详情 + 触发 OTP | 令牌认证 |
| POST | `/api/submit/:token/verify-otp` | 验证 OTP | 令牌认证 |
| POST | `/api/submit/:token/upload` | 上传证据文件 | 令牌认证 |
| GET | `/api/submit/:token/status` | 查看提交状态 | 令牌认证 |

### 11.6 审计接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/audit-logs` | 查询审计日志 | 仅 ADMIN |

查询参数：`entity_type`, `entity_id`, `action`, `campaign_id`, `date_from`, `date_to`, `page`, `limit`

### 11.7 速率限制

| 限制器 | 限额 | 适用范围 |
|--------|------|---------|
| `apiLimiter` | 100 次/分钟 | 所有 /api/* 端点 |
| `authLimiter` | 5 次/分钟 | 登录端点 |
| `portalLimiter` | 30 次/分钟 | 门户端点 |

---

## 十二、Power Automate 集成

### 12.1 当前状态

系统当前运行在 **Mock 模式**下：所有 Power Automate 调用均输出到控制台日志，不发送真实请求。这允许在无 Power Automate 配置的环境下进行完整的开发和测试。

### 12.2 四个 Flow 说明

| Flow | 触发方式 | 用途 | 输入参数 |
|------|---------|------|---------|
| **Flow 1: 发送请求** | HTTP POST Webhook | 发送包含访问链接的邮件给接收方 | to, cc, subject, body (HTML) |
| **Flow 2: 发送 OTP** | HTTP POST Webhook | 发送 OTP 验证码邮件 | to, otp_code |
| **Flow 3: 发送提醒** | HTTP POST Webhook | 发送提醒邮件（支持升级抄送） | to, cc, subject, body, level |
| **Flow 4: 归档文件** | HTTP POST Webhook | 将文件上传到 SharePoint 文档库 | file (Base64), filename, sp_path |

### 12.3 切换到生产模式

1. 在 Power Automate 中创建 4 个 HTTP 触发的即时流
2. 将每个 Flow 的 Webhook URL 填入 `.env` 文件对应变量
3. 将 `NODE_ENV` 设为 `production`
4. 重启后端服务

### 12.4 SharePoint 目录结构

```
SharePoint 站点/
└── Evidencias（文档库）/
    └── {control_code}/
        └── {recipient_email}/
            └── {evidence_name}/
                └── 文件
```

---

## 十三、安全机制

### 13.1 认证体系

| 角色 | 认证方式 | 令牌类型 | 有效期 |
|------|---------|---------|--------|
| 内部用户 | 邮箱 + 密码 → bcrypt 验证 | JWT httpOnly Cookie | 8 小时 |
| 接收方 | 签名令牌 URL | JWT (URL 参数) | 7 天 |
| 接收方 (OTP 后) | OTP 验证通过 | 会话 JWT Cookie | 30 分钟 |

### 13.2 安全措施

- **Helmet**：设置安全 HTTP 头（CSP、X-Frame-Options 等）
- **CORS**：限制允许的前端域名
- **速率限制**：防止暴力攻击（登录 5 次/分钟）
- **bcrypt**：密码哈希加盐
- **SHA-256**：令牌和 OTP 哈希存储
- **httpOnly Cookie**：防止 XSS 窃取令牌
- **输入校验**：Zod 在路由层统一校验
- **SQL 注入防护**：Prisma 参数化查询
- **审计日志**：所有操作不可变记录

---

## 十四、状态机设计

### 14.1 活动状态机

```
          ┌─── 创建 ──→ DRAFT ────── 激活 ──→ ACTIVE
          │                                     │
          │                           所有请求关闭后自动
          │                                     ▼
          │                                COMPLETED
          │                                     │
          │                                  手动归档
          │                                     ▼
          │                                 ARCHIVED
          │
          └──── 只有 DRAFT 状态可以编辑活动信息
```

### 14.2 请求状态机

```
  DRAFT ──发送──→ SENT ──接收方访问──→ IN_PROGRESS
                                          │
                            ┌──── 部分提交 ──→ PARTIAL
                            │                    │
                            └──── 全部提交 ──→ SUBMITTED
                                                 │
                                            审核中 → REVIEW
                                                 │
                              ┌── 全部验证 ──→ READY_TO_CLOSE ──关闭──→ CLOSED
                              │
                              └── 有驳回 ──→ IN_PROGRESS（循环）

  超过截止日期 ──────────────────────────────→ OVERDUE
```

### 14.3 证据状态机

```
  PENDING ──提交文件──→ SUBMITTED ──验证──→ VALIDATED ✓
                              │
                              └── 驳回 ──→ REJECTED
                                              │
                                           补正重置
                                              │
                                              ▼
                                          PENDING（重新循环）
```

---

## 十五、常见问题

### Q1: Docker Desktop 未启动时如何操作？

手动启动 Docker Desktop，然后执行：
```bash
docker-compose up -d db
```

如果端口 5432 被其他 PostgreSQL 占用，修改 `.env` 中的 `DB_PORT` 和 `DATABASE_URL` 使用不同端口（如 5433）。

### Q2: 数据库迁移失败怎么办？

```bash
# 1. 确认数据库正在运行
docker ps --filter "name=db"

# 2. 重新生成 Prisma 客户端
npx prisma generate

# 3. 重新执行迁移
npx prisma migrate dev --name init

# 4. 如需完全重置
npx prisma migrate reset
```

### Q3: 如何修改默认端口？

修改 `.env` 文件：
- 后端：`APP_PORT=新端口号`，同时更新 `APP_BASE_URL`
- 数据库：`DB_PORT=新端口号`，同时更新 `DATABASE_URL`
- 前端：修改 `client/package.json` 中 `dev` 脚本的 `-p` 参数
- API 代理：修改 `client/next.config.js` 中的 `destination` URL

### Q4: 如何添加新用户？

目前可通过 Prisma Studio 或直接操作数据库：
```bash
npx prisma studio
# 浏览器打开 → users 表 → 添加记录
# 密码需要 bcrypt 哈希（可使用在线工具生成）
```

### Q5: Power Automate Mock 模式下会发生什么？

所有邮件发送、OTP 发送、文件归档操作会：
1. 在控制台输出完整的请求参数
2. 返回模拟的成功响应
3. 文件归档返回模拟的 SharePoint 路径

系统功能完全可用，只是不会实际发送邮件或归档文件。

### Q6: 前端开发热重载不工作？

确保使用 `npm run dev` 启动，Next.js 14 内置热重载。如仍不工作：
```bash
# 清除 Next.js 缓存
rm -rf client/.next
cd client && npm run dev
```

### Q7: 如何查看完整的 API 响应？

```bash
# 健康检查
curl http://localhost:3002/health

# 登录获取 Cookie
curl -c cookies.txt -X POST http://localhost:3002/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@firma.com","password":"admin123"}'

# 使用 Cookie 调用其他 API
curl -b cookies.txt http://localhost:3002/api/campaigns
```

### Q8: 服务端口冲突怎么办？

当前配置（适配已有 Docker 容器）：
| 服务 | 端口 | 备注 |
|------|------|------|
| 后端 | 3002 | 避开 3000 (其他项目) |
| 前端 | 3001 | Next.js 开发服务器 |
| 数据库 | 5433 | 避开 5432 (其他 PostgreSQL) |

---

> 📅 文档最后更新：2026-02-20
>
> 📧 种子数据账号：`admin@firma.com` / `responsable@firma.com`（密码均为 `admin123`）
